const testStatus = {
  DRAFT: 'DRAFT',
  NEW: 'NEW',
  OLD: 'OLD',
  REJECTED: 'REJECTED',
  NEED_REVIEW: 'NEED_REVIEW'
}

const testMode = {
  PUBLIC: 'PUBLIC',
  PRIVATE: 'PRIVATE'
}

module.exports = {
  testStatus: testStatus,
  testMode: testMode
}
